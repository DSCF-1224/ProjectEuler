% --- documentclass --- %
\documentclass[article,head_space=25mm,foot_space=30mm,line_length=160mm]{jlreq}

% --- title --- %
\title{Memo of Project Euler}%
\author{DSCF\_1224}%
\date{\today}%

% --- 日本語フォントの設定 --- %
\usepackage{luatexja}% 日本語したい
\usepackage{luatexja-preset}% 源ノ明朝/ゴシックしたい
\usepackage{luatexja-otf}

% --- 数式環境の設定 --- %
\usepackage{amsmath,amssymb}
\usepackage{unicode-math}
	\setmainfont{Asana}%
	% \setmonofont[Ligatures=TeX]{Courier New}%{Inconsolatazi4}
	\setmonofont[Contextuals={Alternate}]{Fira Code}
	\unimathsetup{math-style=ISO,bold-style=ISO}
	\setmathfont{Asana Math} 
	% \setmathfont{STIX Two Math}%
\usepackage{siunitx}
	\sisetup{binary-units=true}
	\sisetup{group-separator={,}}
	\sisetup{inter-unit-product=\ensuremath{\cdot}}
	\sisetup{per-mode=symbol}%
	\sisetup{space-before-unit=true}%
	\DeclareSIUnit{\rpm}{\symup{rpm}}

% --- 相互参照の設定 --- %
\usepackage[pdfencoding=auto]{hyperref}
	\hypersetup{%
		hidelinks,%
		setpagesize=false,%
		pdftitle={Memo of Project Euler},%
		pdfauthor={undefined},%
	}
\newcommand*{\FigrefJpn}[1]{{\gtfamily \figurename}{\bfseries \ref{#1}}}
\newcommand*{\TblrefJpn}[1]{{\gtfamily \tablename}{\bfseries \ref{#1}}}

% --- 図の設定 --- %
\usepackage{xcolor}
	\input{preamble/xcolor.ltx}
\usepackage{tikz}
	\usetikzlibrary{arrows.meta}%
	\usetikzlibrary{calc}%
	\usetikzlibrary{decorations}%
	\usetikzlibrary{decorations.pathmorphing}%
	\usetikzlibrary{patterns}%
	\usetikzlibrary{shapes.arrows}%
	\usetikzlibrary{shapes.geometric}%
	\usetikzlibrary{trees}%
	\usetikzlibrary{positioning}%
	\usetikzlibrary{circuits.logic.US}%
	\usetikzlibrary{shapes.gates.logic.US}%
\usepackage[most]{tcolorbox}
	\tcbuselibrary{breakable}
	\tcbuselibrary{theorems}
	\tcbuselibrary{skins}
	\input{preamble/tcolorbox.ltx}

% --- その他のパッケージ --- %
\usepackage{arydshln}
	\hdashlinewidth=1.5pt
	\hdashlinegap=1.0pt
\usepackage{booktabs}
\usepackage{cancel}
	\input{preamble/cancel.ltx}
\usepackage{capt-of}
\usepackage[inline]{enumitem}
\usepackage{expl3}
\usepackage{longtable}
\usepackage{multicol}
\usepackage{multirow}
\usepackage{rotating}
\usepackage{subcaption}
\usepackage{url}
\usepackage{lipsum}
\usepackage{bxjalipsum}
% \usepackage{makeidx} \makeindex%

% --- 自作 commands --- %
\input{preamble/GlobalMathStyles.ltx}
\input{preamble/GlobalMathSymbols.ltx}
\input{preamble/GlobalTextSymbols.ltx}

% --- jlreq --- %
